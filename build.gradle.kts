// Proyecto raíz — Edge Read Aloud TTS.
//
// IMPORTANTE: con Android Gradle Plugin 9.x el soporte de Kotlin viene
// integrado en el propio AGP (built-in Kotlin). NO se aplica aquí el plugin
// "org.jetbrains.kotlin.android" ni se declara su versión: hacerlo provocaría
// un conflicto de plugin duplicado.

plugins {
    id("com.android.application") version "9.4.0" apply false
}
